import React from "react";

type Props = {};

const Home = (props: Props) => {
  return (
    <div>
      Home <button className="btn btn-primary">Test CSS</button>
    </div>
  );
};

export default Home;
